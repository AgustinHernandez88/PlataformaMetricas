import { Activity, Gauge, Zap, Database } from "lucide-react";
const units = (key: string) => /voltage|voltaje/i.test(key) ? "V" : /current|corriente/i.test(key) ? "A" : /power|potencia/i.test(key) ? "kW" : /energy|energ[ií]a/i.test(key) ? "kWh" : "";
export default function MetricGrid({ total, averages, maximums }: { total: number; averages: Record<string, number>; maximums: Record<string, number> }) { const keys = Object.keys(averages).slice(0, 3); const cards = [{ label: "Registros", value: total.toLocaleString("es-CL"), note: "Lecturas disponibles", Icon: Database }, ...keys.map((key, index) => ({ label: index === 2 ? `${key} máx.` : `${key} promedio`, value: `${index === 2 ? maximums[key] : averages[key]} ${units(key)}`.trim(), note: index === 2 ? "Valor más alto" : "Promedio histórico", Icon: index === 0 ? Gauge : index === 1 ? Activity : Zap }))]; return <section className="grid">{cards.map(({ label, value, note, Icon }) => <article className="card" key={label}><div className="card-header"><span>{label}</span><Icon className="card-icon" size={20}/></div><div className="card-value">{value}</div><div className="card-note">{note}</div></article>)}</section>; }
